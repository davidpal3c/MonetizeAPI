/** Shown beside the input as muted examples (not pre-filled). */

export const FREE_TEXT_INPUT_EXAMPLE =
  "B2B API that returns a company risk score from name and domain for procurement agents. Per-call pricing, low volume, high value.";

export const STRUCTURED_INPUT_EXAMPLE = `POST /company-risk-score
Input: companyName, domain
Output: risk score, sanctions flags
Target users: procurement teams, AI agents
Estimated cost per call: $0.04
Domain: data_api`;
