{
  "name": "monetize-api",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "generate:fixture": "pnpm --filter @monetize-api/core run generate:fixture"
  },
  "engines": {
    "node": ">=20"
  }
}
